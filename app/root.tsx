import Component, {
  CatchBoundary,
  ErrorBoundary,
  meta,
} from "./layouts/document/document.component";
import { loader } from "./layouts/document/document.server";

export { CatchBoundary, ErrorBoundary, meta, loader };
export default Component;
